apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress
  namespace: demo-flask
spec:
  rules:
  - host: flask.matrix
    http:
      paths:
      - path:
        backend:
          serviceName: service-flask-backend
          servicePort: 5000